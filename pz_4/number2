#Дано целое число N (>0). Если оно является степенью числа 3, то вывести TRUE, если не 
#является — вывести FALSE.

try:
    N = int(input("Введите целое число N: "))
    N = N**(1/3)
    # Проверяем, является ли N степенью числа 3
    print(N.is_integer())
except ValueError:
    print(False)
